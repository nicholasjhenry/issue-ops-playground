#! /usr/bin/env elixir

Mix.install([
  {:jason, "~> 1.4"}
])

System.argv() |> Enum.join() |> Jason.decode! |> IO.inspect

"GITHUB_OUTPUT"
|> System.fetch_env!()
|> File.open!([:write, :append])
|> tap(&IO.puts(&1, "foo=bar"))
|> tap(&IO.puts(&1, "baz=qux"))
|> File.close()
