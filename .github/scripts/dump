#! /usr/bin/env elixir

Mix.install([
  {:jason, "~> 1.4"}
])

IO.puts("Hello world!")

System.argv() |> IO.inspect

System.argv() |> Enum.join() |> Jason.decode! |> IO.inspect

Jason.encode!(%{hello: "world"})
